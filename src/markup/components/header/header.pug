include ../logo/logo
include ../header-nav/header-nav
include ../lang/lang

mixin header(data)
    +b.HEADER.header()&attributes(attributes)
        +e.container.container
            +logo()
            +header-nav()
            +lang()
